"""
Prompt configuration for case element extraction.
Each module maps to a section in the CaseDetail "案件要素" tab.
"""

SYSTEM_PROMPT = (
    "你是一位精准的信息提取专家，服务于粤省法智能辅助办案系统。"
    "你的任务是从案件文档中提取关键信息字段。"
    "严格遵循以下规则：\n"
    "1. 只从提供的文档内容中提取，不要编造任何信息\n"
    "2. 如果文档中没有提及该信息，对应字段值设为空字符串 \"\"\n"
    "3. 只返回要求的JSON，不要有任何解释、前缀或后缀\n"
    "4. 提取的信息要精确、简洁，去除多余修饰词"
)

COMPLAINANT_PROMPT = """请从以下案件文档内容中提取「投诉人/投诉企业」的基本信息。

严格遵守以下规则：
1. 提取投诉企业的真实全称、地址、投诉日期，只返回文档中明确写出的信息
2. 判断投诉人是否已向被投诉单位进行过质疑，值必须是"已质疑"或"未质疑"
3. 如果某个字段在文档中找不到，该字段值设为空字符串""
4. 不要返回示例值，不要编造数据

返回格式：
{"companyName": "企业全称","address": "地址","complaintDate": "2024年X月X日格式的日期","hasProtested": "已质疑/未质疑"}
严格只返回一个JSON对象，不要添加任何解释。

=== 案件文档内容 ===
{context}
=== 内容结束 ===

请返回JSON："""

RESPONDENT_PROMPT = """请从以下案件文档内容中提取「被投诉人/被投诉企业」的基本信息。

严格遵守以下规则：
1. 提取被投诉企业的真实全称和地址，只返回文档中明确写出的信息
2. 如果有多个被投诉人/被投诉企业，取第一个或主要的一个
3. 如果某个字段在文档中找不到，该字段值设为空字符串""
4. 不要返回示例值，不要编造数据

返回格式：
{"companyName": "从文档中提取的企业全称，没有则为空字符串","address": "从文档中提取的地址，没有则为空字符串"}
严格只返回一个JSON对象，不要添加任何解释。

=== 案件文档内容 ===
{context}
=== 内容结束 ===

请返回JSON："""

PROJECT_INFO_PROMPT = """请从以下案件文档内容中提取「采购项目相关信息」。

严格遵守以下规则：
1. 只返回文档中明确写出的真实信息，不要编造数据
2. 每个字段提取具体值，如果找不到则设为空字符串""
3. 不要返回示例值

返回格式：
{"projectName": "项目名称","projectCode": "项目编号","biddingCompany": "中标企业全称","purchaser": "采购人全称","agency": "招标代理机构全称"}
严格只返回一个JSON对象，不要添加任何解释。

=== 案件文档内容 ===
{context}
=== 内容结束 ===

请返回JSON："""

COMPLAINT_ITEMS_PROMPT = """请从以下案件文档内容中提取所有「投诉事项」。
每个投诉事项包含标题、投诉内容和法律依据。

严格遵守以下规则：
1. 只提取文档中明确列出的投诉事项，不要编造
2. 标题要简洁概括，内容要完整表达投诉主张
3. 法律依据要精确到条款，如找不到则设为空字符串""
4. 如果文档中没有明确的投诉事项，返回空数组[]
5. 不要返回示例值

返回格式（JSON数组）：
[{"title": "事项标题","content": "具体内容描述","legalBasis": "法律依据，没有则为空字符串"}]
严格只返回一个JSON数组，不要添加任何解释。

=== 案件文档内容 ===
{context}
=== 内容结束 ===

请返回JSON数组："""

SECTION_QUERIES = {
    "complainant": "投诉人 投诉企业 申请人 基本信息 企业名称 地址 投诉日期",
    "respondent": "被投诉人 被投诉企业 被申请人 基本信息 企业名称 地址 单位名称",
    "project_info": "采购项目 项目名称 项目编号 中标企业 采购人 代理机构 招标",
    "complaint_items": "投诉事项 投诉内容 法律依据 违反 规定 请求",
}

SECTION_PROMPTS = {
    "complainant": COMPLAINANT_PROMPT,
    "respondent": RESPONDENT_PROMPT,
    "project_info": PROJECT_INFO_PROMPT,
    "complaint_items": COMPLAINT_ITEMS_PROMPT,
}
